<div class="alert" role="alert">
  <div class="alertContent">
    <strong></strong> <span></span>
  </div>
  
</div>

<script>
  $(document).ready(function(){
    // TODO -- necessary to clear the namespace to not multiassign document listeners. WIERD.



    if (!window.uaf){
      console.log('instantiting uaf');
      window.uaf = new UserAjaxFeedback();
      // var uaf = new UserAjaxFeedback();
    } else {
      console.log('uaf exists, and document ready is running again');
      uaf.pageReload();
    }
    // uaf.init(); 
     
    // console.log(uaf);
  })
</script>