<script type="text/javascript">
  setTimeout(
    function(){
      var a = document.createElement("script");
      var b = document.getElementsByTagName("script")[0];
      // a.src = "<%= root_url %>/mech-bug-tracker.js?projectID=<%= @project.id %>";
      a.src = "<%= root_url %>" + "getform?callback=parseMechBugTrackerScript";
      a.async = true;
      a.type = "text/javascript";
      b.parentNode.insertBefore(a,b)
  }, 1);

  function parseMechBugTrackerScript(r){
    var script = r.js;
    console.log(script);
    new Function(script)(); 
  }
</script>