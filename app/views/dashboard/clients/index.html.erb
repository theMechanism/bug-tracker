<%= button_tag(class: 'btn btn-default newClient') do
  content_tag(:span, 'New Client', class: 'glyphicon glyphicon-plus')
end %>

<% @clients.each do |client| %>
  <%= render 'well', client: client %>
<% end %>

<%= render 'shared/modal' %>

