<h4>Team Workload</h4>
<table class="table">
  <tr>
    <th>Member</th>
    <th># of Bugs Assigned</th>
  </tr>
  <% admins.each do |admin| %>
    <tr>
      <td><%= mail_to(admin.email, admin.name) %></td>
      <td><%= admin.bugs.count %></td>
    </tr>
  <% end %>
</table>